services:
  daily-report-viewer:
    build: .
    container_name: daily-report-viewer
    ports:
      - "8081:80"
    volumes:
      - ./report:/app/report   # SQLite DB 및 리포트 디렉토리 마운트
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Seoul
    restart: unless-stopped
